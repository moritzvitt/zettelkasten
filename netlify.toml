[build]
  command = "npx quartz plugin install --from-config && npm ci --prefix plugins/graph && QUARTZ_BASE_URL=$DEPLOY_PRIME_URL npm run configure-site-url && npm run build"
  publish = "public"

[build.environment]
  NODE_VERSION = "22.16.0"
  NPM_FLAGS = "--include=dev"
